namespace Weariness.Transition
{
    public enum TransitionAnimationType
    {
        OneByOne,        // 한개씩
        OneLineWidth,    // Width로 한줄씩
        OneLineHeight,   // Heigth로 한줄씩
        AdjacentRegions, // 인접한 영역순으로
    }
}